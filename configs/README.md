# Configs

This directory is for versioned, non-secret configuration assets that should live in the repository.

Use it for:

- local example config profiles
- research universe defaults
- risk baseline examples
- future model routing or prompt policy manifests

Do not put secrets here. Secret-bearing runtime values should remain in environment variables or a future secrets system.
