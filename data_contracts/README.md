# Data Contracts

This directory is reserved for future machine-readable contract artifacts, such as versioned JSON Schema manifests, dataset manifests, and interface snapshots.

Day 1 keeps the canonical contract definitions in `libraries/schemas/` and the policy-level contract rules in [`docs/contracts/data_contracts.md`](../docs/contracts/data_contracts.md).
