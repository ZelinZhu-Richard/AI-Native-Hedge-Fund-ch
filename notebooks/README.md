# Notebooks

Notebooks are allowed for exploratory work, but they are not the system of record.

Rules for future notebook usage:

- treat notebooks as exploratory only
- never make untracked dataset mutations from a notebook
- persist reusable logic back into `libraries/` or `services/`
- record data snapshot identifiers for any result worth keeping
